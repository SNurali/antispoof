# HANDOFF — E-GAZ 2 (mTLS / staging / anti-replay), 2026-07-18

## ⚠️ ВНЕ-СКОУПА, НО ВАЖНО: MT5 на yoyo-server остановлен

Нурали сообщил о потере $3000 на реальном счету (16-17.07), попросил отключить боевого скальпера (`sonnet46_scalp_bot_V2`, счёт `252438624`, Exness-MT5Real33, hedging) на `yoyo-server` (172.16.252.32) — том же сервере, где теперь ещё и antispoof-staging.

50 CENT выполнил: `systemctl --user stop/disable` на 5 юнитах (`mt5-xvfb`, `mt5-x11vnc`, `mt5-novnc`, `mt5-wm`, `mt5-terminal`). Конфиг советника/GUI/открытые позиции НЕ трогал.

**Находка:** терминал сам упал ещё 16.07 14:39 (баг ordering-cycle в systemd-юнитах, автозапуск ломался на каждом ребуте) — новых сделок физически не было двое суток до отключения. Вчерашний убыток — вероятно просадка по уже открытой позиции (в логе на 16.07 13:16 виден `sell 2.84 XAUUSD` + `buy 2.84 XAUUSD` в hedging-режиме, не факт что закрылось), а не новый вход робота.

Актуальный баланс/открытые позиции — смотреть в приложении/кабинете Exness напрямую, терминал специально не запускали. Rollback (когда понадобится) — команды в отчёте агента; сначала почини ordering-cycle баг в юнитах, иначе автозапуск на ребуте опять сломается сам.


Причина: возможное отключение света у Нурали, сессия может оборваться. Этот файл — точка возврата для следующей сессии.

## Что уже сделано и запушено (не трогать, готово)

- `~/E-GAZ/antispoof` — main на GitHub (`SNurali/antispoof`), актуальный коммит `ad977e2`:
  - Phase 0 + RedisSessionStore (`acb2681`)
  - IP-allowlist proxy-trust fix (`35a1560`)
  - Anti-replay timestamp window, `REPLAY_PROTECTION_ENABLED=False` по умолчанию (`ad977e2`)
  - 218/218 тестов зелёные, всё запушено на `origin/main` — подтверждено вручную.
- `low_quality` verdict удалён из `/liveness/verdict` (подтверждено 2PAC по коду, `app/main.py:1159`) — это было сделано раньше, не путать с открытыми задачами.
- mTLS-конфиги готовы в `deploy/mtls/` (BUSTA RHYMES): `generate-mtls-ca.sh`, `nginx-antispoof-mtls.conf`, `antispoof-loopback.service`, `MTLS-DEPLOY-CHECKLIST.md`, `MTLS-PARTNER-HANDOFF.md`. `deploy/mtls/output/` пуст — сертификаты ЕЩЁ НЕ сгенерированы.
- 2PAC сверил 6 пунктов контракта партнёра (Рустам/Наташа) с реальным кодом — полный вердикт отправлен в мост `egaz-mobile`. Ключевая поправка: `liveness_token`/TTL/409/401 — это код партнёра (Laravel), НЕ наш antispoof-репозиторий. Redis session store у нас уже готов и протестирован — их формулировка "ждём вашего Redis" устарела с нашей стороны.
- Архитектурная схема (PDF + Artifact) отправлена Нурали: `~/E-GAZ/E-GAZ2-Architecture-2026-07-18.pdf`.

## Открытые хвосты (см. также TaskList текущей сессии, задачи #1-#4)

1. **mTLS.** Раскатывать будут САМИ Умид ака + Наташа на своей стороне (не мы — у нас нет и не будет SSH к egaz-02.uz, подтверждено 50 CENT: DNS не резолвится, IP нигде не записан, ключ не добавлен). Мы им отправили в мост `egaz-mobile` вопрос: нужен IP/hostname egaz-02.uz для генерации серверного сертификата. **Нурали генерирует root CA + серверный + клиентский сертификат ЛИЧНО, оффлайн** (`deploy/mtls/generate-mtls-ca.sh`), НЕ через агента — это ключевое security-решение, ключ CA не должен попасть в облако/на сервер. Ждём ответа партнёра в мосте.

2. **Staging-стенд для теста партнёра** — ГОТОВ, поднят 50 CENT, проверен вживую (`/health` 200, `/liveness/challenge` 200, без токена 401).
   - URL: `https://antispoof-staging.smartwash.uz/` (публичный, через Cloudflare Tunnel, реальный HTTPS)
   - Auth: заголовок `X-Service-Token` — токен лежит ТОЛЬКО в `.env` на сервере (`yoyo@172.16.252.32:/home/yoyo/antispoof-staging/.env`), в чат/мост НЕ печатался, партнёру ещё не отправлен.
   - Развёрнут на `yoyo-server` (172.16.252.32) через отдельный Docker Compose (`antispoof-staging`, порт 18090 loopback-only, отдельный не-прод токен).

   ⚠️ **ДВА РЕШЕНИЯ ВЛАДЕЛЬЦА ДО ОТПРАВКИ ПАРТНЁРУ:**
   1. **yoyo-server — это тот же сервер, где живёт боевой MT5-скальпер с реальными деньгами** (sonnet46_scalp_bot_V2, реал $100, см. память `mt5-server-deployment`). 50 CENT об этом явно не упомянул (возможно не знал контекста) — изоляция только докер+порт+токен, не отдельная VM. Нужно решить: оставляем на этом сервере (изоляция ресурсов Docker вроде достаточна для staging-нагрузки) или переезжаем на отдельный VPS (~$5-6/мес, 50 CENT говорит ~15 минут переехать, стек уже готов).
   2. **Домен одолжен у другого бизнеса** — `antispoof-staging.smartwash.uz` резолвится ПУБЛИЧНО всему интернету (защита только токеном). Это чужой поддомен (SmartWash). Если нужен нейтральный домен под E-GAZ — 10 минут дела, но нужно сказать 50 CENT.

   Также **не закоммичен фикс** в `~/E-GAZ/antispoof/Dockerfile`: `libgl1-mesa-glx` → `libgl1` (старый пакет пропал из свежего `python:3.11-slim`, реальный баг сборки, не выдумка) — нужен твой ОК на коммит, иначе следующая чистая сборка (в т.ч. потенциально на проде) сломается так же.

   **Партнёру URL+токен ещё НЕ отправлены** — ждут решения по пунктам 1-2 выше.

3. **Anti-replay** (`REPLAY_PROTECTION_ENABLED` в `app/config.py:74`) — включать `True` ТОЛЬКО после того, как mTLS-канал живой и партнёр подтвердит, что шлёт заголовок `X-Request-Timestamp`. Пока трогать не нужно.

4. **Q9** — дата включения тоггла liveness. Решение Нурали + Рустам (калибровка энтропии challenge + APCER/BPCER). Не инженерная задача, просто держать в уме как внешний блокер без даты.

## Мост agent-mesh (канал `egaz-mobile`)

Последние отправленные нами сообщения (по порядку):
1. Вопрос про IP/SSH к egaz-02.uz для mTLS.
2. Уточнение: раскатку делают они сами, нам нужен только IP/hostname для сертификата.
3. Развёрнутый ответ 2PAC по 6 пунктам их контракта (abonent_kod, liveness_token-зона, SERVICE_TOKEN/HTTPS, 1-воркер/Redis).

При возврате в сессию — **первым делом проверить мост** (`mesh_inbox` / `mesh_history` канал `egaz-mobile`) на предмет ответа от Наташи/Умида по IP egaz-02.uz — это разблокирует всю цепочку mTLS.

## Порядок действий при продолжении

1. Проверить мост — пришёл ли IP/hostname от партнёра.
2. Проверить, вернулся ли 50 CENT по staging-стенду (или перезапустить разведку).
3. Если IP пришёл — напомнить Нурали сгенерировать root CA + сертификаты локально (это его шаг, не агента).
4. Как только сертификаты готовы — отдать партнёру через мост (серверный сертификат + конфиги deploy/mtls/), договориться о дате переключения.
5. После подтверждения, что mTLS живой и партнёр шлёт X-Request-Timestamp — включить REPLAY_PROTECTION_ENABLED, прогнать 218 тестов, закоммитить.
